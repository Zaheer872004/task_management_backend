'use strict';

export const TASK_STATUS = Object.freeze({
  TODO: 'todo',
  IN_PROGRESS: 'in_progress',
  DONE: 'done',
});

export const TASK_TYPE = Object.freeze({
  PERSONAL: 'personal',
  ASSIGNED: 'assigned',
});
